import { AbstractFile } from './AbstractFile.js';
import { Iterator } from './Iterator/index.js';
import { EncryptionHeader } from '../common/encryption.js';
export declare class EncryptedFile extends AbstractFile {
    inner: AbstractFile;
    key: Uint8Array;
    header: EncryptionHeader;
    constructor(inner: AbstractFile, key: Uint8Array, header: EncryptionHeader);
    protected createFragment(offset: number, size: number, _paddedSize: number): AbstractFile;
    readFromFile(start: number, end: number): Promise<{
        bytesRead: number;
        buffer: Uint8Array;
    }>;
    iterateWithOffsetAndBatch(offset: number, batch: number, flowPadding: boolean): Iterator;
}
export declare class EncryptedFileFragment extends AbstractFile {
    parent: EncryptedFile;
    constructor(parent: EncryptedFile, offset: number, size: number);
    protected createFragment(_offset: number, _size: number, _paddedSize: number): AbstractFile;
    readFromFile(start: number, end: number): Promise<{
        bytesRead: number;
        buffer: Uint8Array;
    }>;
    iterateWithOffsetAndBatch(offset: number, batch: number, flowPadding: boolean): Iterator;
}
export declare function newSymmetricEncryptedFile(inner: AbstractFile, key: Uint8Array): EncryptedFile;
export declare function newEciesEncryptedFile(inner: AbstractFile, recipientPub: Uint8Array | string): EncryptedFile;
//# sourceMappingURL=EncryptedFile.d.ts.map